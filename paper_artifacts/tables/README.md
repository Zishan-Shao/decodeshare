# Tables

Place final paper tables and compact regenerated table outputs here.

Preferred formats:

- `.tex` for paper-ready LaTeX tables.
- `.csv` for machine-readable summaries.
- `.md` for audit summaries when a table combines multiple sources.
