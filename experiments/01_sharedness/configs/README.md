# Sharedness Configs

These config files are human-readable parameter records for paper reproduction. The current scripts use argparse; treat these files as the source of truth for camera-ready settings and translate them to CLI flags.

- `full_benchmark.yaml`: H1 existence/full-benchmark settings.
- `diagnostics.yaml`: activation collection and diagnostic figure settings.
